// Scaffold placeholder — Contact page + inquiry form (client-side Formspree
// submit, no backend) is built next. See CLAUDE.md and app/CLAUDE.md.
export default function ContactPage() {
  return <main>Contact — scaffold in progress.</main>;
}
